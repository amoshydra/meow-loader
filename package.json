{
  "name": "meow-loader",
  "version": "0.1.0",
  "description": "Download M3U8/HLS video streams and merge into MP4",
  "type": "module",
  "bin": {
    "meow-loader": "./dist/index.mjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "prepack": "pnpm run build",
    "start": "node dist/index.js",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsdown": "^0.21.9",
    "typescript": "^5",
    "vitest": "^2.0.0"
  }
}
